import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        margin:10,
    },
    title:{
        color:'white',
        margin:10,
        fontSize:23,
        fontWeight:"bold", 
    }
})

export default styles;